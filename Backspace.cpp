#include <iostream>
#include <vector>
#include <string>

/*
using namespace std;
int main()
{
    string input;
    getline(cin, input);

    vector<char> result;
    int num = 0;
    int lastindex = 0;
    for(int i = 0; i < input.length(); i++)
    {
        if (input[i] != '<')
            result.push_back(input[i]);
        else
            result.pop_back();
    }
    for(int i = 0; i < result.size(); i++)
    {
        cout << result[i];
    }
    cin >> input;
}
*/