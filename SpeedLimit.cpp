#include <iostream>
#include <vector>
#include <string>

/*
using namespace std;
int main()
{
    int n, s, t;
    while(!cin.eof())
    {
        int distance = 0;
        int lastT = 0;
        cin >> n;
        if(n == -1)
        {
            return 0;
        }
        for(auto i = 0; i < n; i++)
        {
            cin >> s >> t;
            distance += (s*(t - lastT));
            lastT = t;
        }
        cout << distance << " miles" << endl;
    }
    return 0;
}
*/