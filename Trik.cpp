#include <iostream>
#include <vector>
#include <string>

/*
using namespace std;

struct cup
{
    bool hasBall = false;;
};

int main()
{
    cup cup1;
    cup cup2;
    cup cup3;

    cup1.hasBall = true;

    string commands;
    getline(cin, commands);

    for(int i = 0; i < commands.length(); i++)
    {
        if(commands[i] == 'A')
        {
            bool temp = cup1.hasBall;
            cup1.hasBall = cup2.hasBall;
            cup2.hasBall = temp;
        }
        else if(commands[i] == 'B')
        {
            bool temp = cup2.hasBall;
            cup2.hasBall = cup3.hasBall;
            cup3.hasBall = temp;
        }
        else if(commands[i] == 'C')
        {
            bool temp = cup1.hasBall;
            cup1.hasBall = cup3.hasBall;
            cup3.hasBall = temp;
        }
    }

    if(cup1.hasBall)
    {
        cout << 1;
    }
    else if(cup2.hasBall)
    {
        cout << 2;
    }
    else if(cup3.hasBall)
    {
        cout << 3;
    }
    return 0;
}
*/